# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: bookhunt_app_development
  username: root
  password: password
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:

  adapter: mysql
  database: bookhunt_app_test
  username: root
  password: password
  host: localhost
  
production:

  adapter: mysql
  database: bookhunt_app_production
  username: root
  password: password
  host: localhost